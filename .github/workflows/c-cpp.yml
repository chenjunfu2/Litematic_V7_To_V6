name: C/C++ CI

on:
  workflow_dispatch:
  #push:
  #  branches: [ "master" ]
  #pull_request:
  #  branches: [ "master" ]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        compiler: [gcc, clang]
      fail-fast: false

    runs-on: ${{ matrix.os }}
      
    steps:
    - uses: actions/checkout@v6

    - name: Setup GCC
      if: matrix.compiler == 'gcc'
      uses: egor-tensin/setup-gcc@v1
      with:
        version: latest

    - name: Setup Clang
      if: matrix.compiler == 'clang'
      uses: egor-tensin/setup-clang@v1
      with:
        version: latest

    - name: Build (Unix)
      if: runner.os != 'Windows'
      run: |
        chmod +x cmake-build.sh
        ./cmake-build.sh
      shell: bash

    - name: Build (Windows)
      if: runner.os == 'Windows'
      run: cmake-build.bat
      shell: cmd
    
    - name: List Dir (Unix)
      if: runner.os != 'Windows'
      run: find . -type f | sort
      shell: bash

    - name: List Dir (Windows)
      if: runner.os == 'Windows'
      run: Get-ChildItem -Recurse | Select-Object FullName | Sort-Object FullName
      shell: pwsh
